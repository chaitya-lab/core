"""Chaitya Core — Event-driven microkernel for personal automation.

The kernel provides stable primitives — command dispatch, session management,
event bus, pipeline mechanics, adapter registry, and persistent store — and
nothing else. Every capability is an independently installable adapter.
"""

__version__ = "0.1.0a1"

