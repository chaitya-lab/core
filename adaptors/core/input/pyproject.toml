[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "chaitya-adapter-input"
version = "0.1.0a1"
description = "First-party L0 ingest adapter for Chaitya Core"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/chaitya_adapter_input"]
