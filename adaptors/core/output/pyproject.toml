[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "chaitya-adapter-output"
version = "0.1.0a1"
description = "First-party L2 output adapter for Chaitya Core"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/chaitya_adapter_output"]